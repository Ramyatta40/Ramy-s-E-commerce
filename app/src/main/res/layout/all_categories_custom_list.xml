<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/view"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<ImageView
    android:id="@+id/all_cat_image"
    android:layout_width="match_parent"
    android:layout_height="@dimen/_100sdp"
    android:background="@drawable/rectangle2_shop_image"
    android:backgroundTint="@color/purple_200"
    android:layout_margin="@dimen/_10sdp"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="@id/all_cat_image"
    />
    <TextView
        android:id="@+id/all_cat_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="cat name"
        android:textStyle="bold"
        android:textSize="@dimen/_18ssp"
        android:textColor="@color/white"
        app:layout_constraintTop_toTopOf="@id/all_cat_image"
        app:layout_constraintStart_toStartOf="@id/all_cat_image"
        app:layout_constraintEnd_toEndOf="@id/all_cat_image"
        app:layout_constraintBottom_toBottomOf="@id/all_cat_image"
        />
</androidx.constraintlayout.widget.ConstraintLayout>